# Application configuration
app.name=template_java_maven
app.version=1.0.0
app.description=Template repository for Java projects with Maven

# Logging configuration
logging.level.com.example=DEBUG
logging.level.root=INFO

# Development configuration
app.env=development